---
phase: 1
plan: 2
wave: 1
---

# Plan 1.2: Database Models & Authentication Setup

## Objective
Establish the MongoDB schema logic (Mongoose schemas) matching the specification and create the Authentication service (`login` / `me` API).

## Context
- .gsd/SPEC.md
- .gsd/ARCHITECTURE.md
- backend/models/ (New directory)

## Tasks

<task type="auto">
  <name>Create Data Models</name>
  <files>
    backend/models/User.ts
    backend/models/Intern.ts
    backend/models/Project.ts
    backend/models/Team.ts
    backend/models/Task.ts
  </files>
  <action>
    - Ensure Mongoose schema represents all fields accurately defined in `SPEC.md`. 
    - Include `Timestamps` in Schemas. 
    - Establish refs (ObjectId -> User/Project).
    - Make sure `email` uniquely indexed on `User` and `uniqueID`/`email` uniquely indexed on `Intern`.
  </action>
  <verify>grep "mongoose.Schema" "backend/models/User.ts"</verify>
  <done>Mongoose models for all five core schemas exist.</done>
</task>

<task type="auto">
  <name>Authentication Endpoints & Service</name>
  <files>
    backend/routes/authRoutes.ts
    backend/controllers/authController.ts
    backend/middleware/authMiddleware.ts
  </files>
  <action>
    - In `authMiddleware.ts`: Create JWT verification middleware taking HTTP Bearer header. Add role checking (Admin vs Manager).
    - In `authController.ts`: Write a login controller checking `User` with password vs `bcrypt` comparison.
    - Write a `me` controller returning token payload.
    - Export routes via Express Router.
    - Run DB seed or initial checks allowing easy creation of at least one `admin` for testing.
  </action>
  <verify>grep "Router()" "backend/routes/authRoutes.ts"</verify>
  <done>Login route tests user/password via bcrypt.</done>
</task>

## Success Criteria
- [ ] Users, Interns, Projects, Teams, Tasks models reflect SPEC exactly.
- [ ] Auth POST/GET methods defined and active in Express app router.
