---
phase: 1
plan: 3
wave: 2
---

# Plan 1.3: Frontend Layout & Login View

## Objective
Establish the primary router and login authentication wrapper, including an entry point that provides a clean user interface utilizing TailwindCSS mapping to React state.

## Context
- .gsd/SPEC.md
- frontend/src/

## Tasks

<task type="auto">
  <name>React Context & Router Configuration</name>
  <files>
    frontend/src/context/AuthContext.tsx
    frontend/src/App.tsx
    frontend/src/components/layout/ProtectedRoute.tsx
  </files>
  <action>
    - Setup `AuthContext` to store the decoded JWT (`user`) and token in localStorage. Provide `login`, `logout` variables.
    - Export custom `useAuth` hook.
    - Configure React Router `App.tsx` handling wildcards `*` for 404.
    - Create a wrapper `ProtectedRoute` managing RBAC logic if missing valid role → Redirect to `/login`.
  </action>
  <verify>grep "AuthContext.Provider" "frontend/src/context/AuthContext.tsx"</verify>
  <done>Router wraps private dashboards and allows only authenticated roles.</done>
</task>

<task type="auto">
  <name>Login Interface Development</name>
  <files>
    frontend/src/pages/Login.tsx
    frontend/src/components/ui/Button.tsx
    frontend/src/components/ui/Input.tsx
  </files>
  <action>
    - Ensure Tailwind design achieves "Premium Designs" criteria.
    - Create reusable `<Button>` and `<Input>` components.
    - Set up a Login screen handling state (email, password).
    - Call backend login API. Using `axios`, on success update context and navigate. Show loading spinners & toast errors appropriately. 
  </action>
  <verify>grep "type='submit'" "frontend/src/pages/Login.tsx"</verify>
  <done>Login form is completely styled and triggers auth APIs via Context.</done>
</task>

## Success Criteria
- [ ] Router protects Dashboard interfaces ensuring users hit the Login route.
- [ ] Login Form completes network request correctly checking database values in Mongo.
