---
phase: 2
plan: 2
wave: 2
---

# Plan 2.2: The Manager Dashboard & React Query Integration

## Objective
Wire up the frontend `/dashboard` route with Axios fetching hooks and build a stunning Manager UI that displays the current projects they own.

## Context
- .gsd/SPEC.md
- frontend/src/components/Layout/PagesLayout.jsx
- frontend/src/App.jsx

## Tasks

<task type="auto">
  <name>Construct Manager Dashboard View</name>
  <files>
    frontend/src/pages/Dashboard.jsx
    frontend/src/App.jsx
  </files>
  <action>
    Deploy a visually stunning (Tailwind + Framer Motion) Dashboard.
    - Fetch and display the Logged in user's name via `useAuth()`.
    - Present a "Create New Project" button trigger.
    - Inject `Dashboard` correctly into `App.jsx` under the `ProtectedRoute` replacing the `Dashboard Placeholder` div.
  </action>
  <verify>Visit `localhost:5173/dashboard` and verify the UI appears successfully</verify>
  <done>Clean layout rendered dynamically.</done>
</task>

<task type="auto">
  <name>Form Actions & Axios API Call</name>
  <files>frontend/src/pages/Dashboard.jsx</files>
  <action>
    Hook a state-managed form for Project creation directly firing Axios POSTs to `/api/v1/tms/project`.
    - Include title, description, and deadline date.
    - Wire `useEffect` to fetch existing projects onto the screen so the user physically verifies the read/write lifecycle.
  </action>
  <verify>React logs show successful POST dispatch resulting in a 200/201 Success header.</verify>
  <done>Frontend correctly connects and interacts with the Project controllers crafted in Plan 2.1.</done>
</task>

## Success Criteria
- [ ] UI relies on modern styling.
- [ ] Manager can create a structured project, seeing it rendered immediately.
